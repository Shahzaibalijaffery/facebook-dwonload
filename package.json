{
  "name": "facebook-video-downloader",
  "version": "1.0.0",
  "description": "Chrome extension to download Facebook videos",
  "scripts": {
    "build": "node build.js",
    "bundle": "npm run build && node bundle.js"
  },
  "devDependencies": {
    "terser": "^5.31.0",
    "archiver": "^7.0.1"
  }
}
